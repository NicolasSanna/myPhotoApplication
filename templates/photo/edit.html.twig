{% extends 'base.html.twig' %}

{% block title %}Edit Photo{% endblock %}

{% block body %}
    <h1>Éditer la Photo</h1>

    {{ include('photo/_form.html.twig', {'button_label': 'Modifier'}) }}

    <a href="{{ path('app_photo_index') }}">Revenir à la liste</a>

    {{ include('photo/_delete_form.html.twig') }}
{% endblock %}
