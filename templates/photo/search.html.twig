{{ form_start(form, {'action': path('app_photo_index_autocomplete'), 'method': 'POST'}) }}
    {{ form_widget(form) }}
    <button class="btn">{{ button_label|default('Rechercher') }}</button>
{{ form_end(form) }}